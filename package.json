{
  "name": "moon-auto-detailing-booking",
  "private": true,
  "type": "module",
  "dependencies": {
    "googleapis": "^144.0.0"
  }
}

{
  "dependencies": {
    "resend": "^3.2.0"
  }
}

catch (err) {
  console.error(err);
  return res.status(500).json({
    ok: false,
    error: err.message,
  });
}
